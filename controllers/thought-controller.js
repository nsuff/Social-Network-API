const { Thought, User } = require('../models');

const thoughtController = {
    // get all thoughts

    // create thought

    // update thought

    // delete thougth

    // add reply

    // delete reply
}